name: Deploy to Azure

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 14

      - name: Install Dependencies
        run: npm install

      - name: Build Angular App
        run: npx ng build

      - name: Upload to Azure WebApp using FTP
        uses: ercindedeoglu/ftp-transfer-action@v2
        with:
          server: ${{ demotest.ftp.azurewebsites.windows.net}}
          username: ${{megaclaim__test }}
          password: ${{ r51i2GeuirQzjAg4lkKXX2n8wHBNHmud2qm6GAu1LoFR31hAnmdNM0x6eArg}}
          local-dir: ./dist
          remote-dir: ${{ /site/wwwroot }}
          fail-on-warnings: true
